name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  check:
    name: Check & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: viewer

      - name: Checkout crust
        uses: actions/checkout@v4
        with:
          repository: isene/crust
          path: crust

      - name: Checkout glow
        uses: actions/checkout@v4
        with:
          repository: isene/glow
          path: glow

      - name: Checkout highlight
        uses: actions/checkout@v4
        with:
          repository: isene/highlight
          path: highlight

      - uses: dtolnay/rust-toolchain@stable

      - name: Build
        working-directory: viewer
        run: cargo build --release

      - name: Binary size
        working-directory: viewer
        run: ls -lh target/release/viewer
